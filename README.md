# docker-nginx-www-http-redirects

Simple docker container to be run as a redirect service, redirecting http -> https and www -> root.

Also supports adding a custom.conf file for server blocks.

Clone/fork and build.
